<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:label="Listy zadań"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_exerciseDetailFragment"
            app:destination="@id/exerciseDetailFragment">
            <argument
                android:name="exerciseList"
                app:argType="com.example.a3_lista.ui.ExerciseList"/>
        </action>
    </fragment>

    <fragment
        android:id="@+id/exerciseDetailFragment"
        android:name="com.example.a3_lista.ui.ExerciseDetailFragment"
        android:label="Exercise Detail Fragment">
        <argument
            android:name="exerciseList"
            app:argType="com.example.a3_lista.ui.ExerciseList"/>
    </fragment>
</navigation>
